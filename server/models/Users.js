const { DataTypes } = require("sequelize"); const { sequelize } = require("../config/database"); const Users = sequelize.define("Users", {gid: { type: DataTypes.STRING, allowNull: false, validate: { notEmpty: { msg: "Please provide gid" } } },username: { type: DataTypes.STRING, allowNull: false, validate: { notEmpty: { msg: "Please provide username" } } }, }, { tableName: "userss", timestamps: true }); module.exports = Users;